version: '1.1'
services:
  events-app:
    image: supradeux/events-app:latest
    ports: 
      - "8080:8080"
    environment:
      - JAEGER_ENDPOINT=http://docker.for.mac.localhost:14268/api/traces
    networks:
      - jaeger-example

networks:
  jaeger-example:
